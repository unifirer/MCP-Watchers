# 2026-09-20

## 00:05 — Verified graphiti architecture; proved Toolport can skip the :8004 proxy

User asked (a) does the launcher still use `J:\audio\shared\graphiti\mcp_proxy.py`,
(b) verify the docker→:8002 / proxy→:8004 / registry→:8004 claim, (c) investigate
whether Toolport can talk **directly** to the container.

**Verified (a)/(b) — all true, one wording fix:**
- Launcher `###1` lines 4154–4250 spawn `mcp_proxy.py` as the :8004 adapter; supervisor at 4545;
  README 29–40 agrees; registry.json:487 = `http://127.0.0.1:8004/mcp`; scheduled task
  `GraphitiProxy8004` also runs it.
- Live: `graphiti-mcp` container up (0.0.0.0:8002->8000, healthy); :8002 and :8004 both OPEN.
- Live `initialize` to :8004 returned `Mcp-Session-Id: stateless` + `application/json`,
  `serverInfo.name = "Graphiti Agent Memory"` → bridge really does SSE→JSON + fixed session id.
- Correction: a stateless POST to :8002 with **no** session id returns **HTTP 400
  "Missing session ID"**, not the docstring's "404 Session not found" (that is the
  unknown/invalid-id case). Prose should say 400.

**Answered (c) — YES, direct connection works. The proxy is redundant.**
- Gateway binary scan found a **client-side** header set in 1.17/1.18/1.19:
  `Content-Type, Accept: application/json, text/event-stream, MCP-Protocol-Version,
  Mcp-Session-Id, Authorization` → Toolport does its own session handling and accepts SSE.
  Active version is **1.18.0** (`bin/gateway-manifest.json`).
- Decisive test, no live config touched: copied registry.json → `C:/Temp`, changed the
  graphiti url to `http://127.0.0.1:8002/mcp`, renamed the id to `graphiti-direct`, and ran
  `toolport-gateway-1.18.0.exe` with `TOOLPORT_REGISTRY` pointed at the copy.
  Result: **`connected 'graphiti-direct' (13 tools)`**.
- Control (url → dead :8999): `'graphiti-direct' failed: transport error ... os error 10061`
  → the success was a genuine live handshake, not a cached tool list.
- First probe attempt failed with `Corrupt registry: missing field \`version\`` — a minimal
  registry must carry top-level `version`.

**Container stateless route (investigated, not needed):** SDK supports `stateless_http` /
`json_response`, but `FastMCP.__init__` passes both as explicit `False` into `Settings`, and
pydantic-settings gives init kwargs priority over `FASTMCP_*` env → env override will NOT work;
would require patching server code (lost on image update).

**Stale memory corrected:** MEMORY.md said *"never repoint the registry at :8002"* — false as of
1.18.0. Rewrote MEMORY.md (also consolidated: 9.7 KB → ~7 KB, deduped env/sandbox section).

## 00:40 — APPLIED: :8004 retired, Toolport direct to :8002

User said continue, so the change went in. Four owners had to be handled — the launcher was
only one of them:

1. **registry.json** graphiti url → `http://127.0.0.1:8002/mcp` (single occurrence, asserted).
   Backup: `registry.json.bak-direct8002-20260920`. Verified against the exact final config
   with the isolated probe: `connected 'graphiti' (13 tools)`.
2. **`C:\Users\yuni\.local\bin\mcp-http-watchdog.ps1`** — removed its `Port = 8004` target
   (backup `.bak-20260920`). **This one would have resurrected the proxy every 60 s** if missed.
   Also fixed its stale header ("every 5 minutes" → 1 minute).
3. **Launcher `###1...ps1`** — deleted the :8004 start-job block (97 lines),
   `Start-GraphitiMcpProxyBackend` (29 lines), both dispatch `elseif`s, the sup-log var and the
   `Start-BackendSupervisor` call, and the `$null` handle init. Updated 6 stale comments.
   Kept the :8003 **embed** proxy untouched.
4. **Scheduled task `GraphitiProxy8004`** — **disabled** (not deleted): rollback is
   `Enable-ScheduledTask -TaskName 'GraphitiProxy8004'`.

**Tests:** launcher gate `run_launcher_tests.ps1 -SkipSmoke` = **149 pass / 0 fail** (5 skip);
`backend_sweep_safety.tests.ps1` = **8 pass / 0 fail**. Launcher parses clean (25,730 tokens).
No test referenced :8004, and `launcher_proxy_wiring` turned out to be the litellm :11436/:4000
proxy, unrelated.

**Running Pester here:** the pinned 3.4.0 is invisible to PS 7 (PSModulePath carries a literal
unexpanded `%ProgramFiles%`). Import it by explicit path:
`Import-Module 'C:\Program Files\WindowsPowerShell\Modules\Pester\3.4.0\Pester.psd1'` — then
`Invoke-Pester -PassThru` works.

**Still live:** the existing :8004 python proxy is still running and the current gateway still
uses it (registry is read at gateway start). Nothing will restart it now; it dies on reboot.
Next gateway start picks up :8002.

Committed 2026-09-20 00:54 as `bd9c4a1` on `main` — 4 files, +159/-221 (launcher, README,
this log, MEMORY.md). Only those four were staged; the pre-existing 2026-09-19 changes
(`tests/launcher_tests.ps1`, `tasks.txt`, `.repowise/*`, `.claude/*`, `.gitignore`) were left
alone and are still uncommitted. **Pushed to github** (e758f6b..bd9c4a1) with
`git -c credential.helper= -c credential.helper='!gh auth git-credential' push github main`.
**GitLab NOT pushed** — gitlab main is still e758f6b. The `store --file=` helper never supplied
a username (`fatal: could not read Username for 'https://gitlab.com': terminal prompts
disabled`), in both URL form and stanza form; the URL-embedded-token fallback was SIGTERMed by
the sandbox. Retry from a non-sandboxed shell.
The gitlab PAT from `declick describe gitlab` got echoed into session output (it is embedded in
the adapter `source` string, so a path-based redaction misses it; `toolport_status` prints it
again in the gitlab server line) — rotate it.

## 01:05 — retirement complete; proxy process stopped

`toolport_status` on the live gateway now reports
**`Graphiti MCP [http] http://127.0.0.1:8002/mcp`** with 13 tools — so the flip is live, not
just staged. The orphaned proxy (pid 19848, `python.exe ...\mcp_proxy.py 8004`, started
19/09 23:19) was then terminated; :8004 no longer listens and nothing can restart it (launcher
block gone, watchdog target gone, task disabled). No graphiti failures in gateway.log after
that. :8002 is the only graphiti path now.

Rollback if ever needed: `C:\Python314\python.exe J:\audio\shared\graphiti\mcp_proxy.py 8004`,
then point the registry url back at `http://127.0.0.1:8004/mcp`.
