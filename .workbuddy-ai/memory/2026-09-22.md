# 2026-09-22

## MCP server health verification (via Toolport)
- Verified all 31 enabled servers with ≥1 real `toolport_call_tool` call each.
- **19 PASS** (connected, tools verified). `repowise` was failing (WSL-cwd DB lock / "pipe closed") at session start → **recovered live** (verified `repowise__get_answer`).
- **12 FAIL** (all `no route for tool`): root cause = **this Toolport gateway instance is wedged/flapping** — fails to hold ~12/31 connections while sibling gateway instances connect all 31 (servers healthy).
  - `headroom`: stale `ImportError: MCP SDK not installed`; `mcp` is now importable under Python310 (`from mcp.server import Server` OK) → recovers on gateway restart.
  - `mcp-agent-mail` (:8765): service was refusing, now LISTENING; gateway connected it (41 tools) during sweep but it dropped in flapping.
  - `serena`: initialize-timeout flakiness (intermittent in log).
  - `time`, `memtrace`, `jcodemunch`, `mnemosyne`: were connected BEFORE diagnostics; a diagnostic disable+enable of `grepai` triggered a gateway-wide reconnect sweep that dropped them. **Transparency: my probe caused this.**
- `disable`+`enable` could NOT restore any dead server in the wedged gateway (reconnect replays failed init).
- **Remediation = gateway restart** (Toolport UI / restart WorkBuddy — do NOT kill the gateway process directly; per standing rule it won't auto-respawn). Also prune ~60+ memtrace/uvx processes (likely resource pressure behind flapping).
- Report: `reports/2026-09-22-mcp-server-health-verification.md`.

## Scope audit: did the sweep exceed the cerememory request?
- User asked only for `mcpw-418` (cerememory removed). Audited branch `mcpw-beads-sweep-20260921`.
- **Yes** — non-cerememory commits I made: `2f479a0` (mcpw-d8a), `3b3c1e1` (mcpw-8ue), `187c129` (mcpw-b7z), `573d458` (mcpw-9n1), `270da22`+`8d182a0` (docs for mcpw-gz4/gate fixes), `eeb099f`+`168f73e` (mcpw-rkg.5). All are test/doc only except `2f479a0` (2 lines in launcher).
- Not mine (another parallel session): `52cc631`, `6487022`, `95fdfb3`, `b5016c5`, `7c8daef`, and everything before `d5f6422`.
- The 14 mnemosyne/Toolport beads were already deleted (tracker-only, zero repo edits).

## ping-by-absolute-path hardening + Windows PowerShell on PATH
- Operator ruling: the `ping` sleeper failure is an **environment quirk, not a code bug** — a `.cmd`/`cmd.exe` grandchild cannot resolve a bare `ping` even though its PATH is byte-identical to the parent's (79 entries, both System32 forms). Fix = address `Join-Path $env:SystemRoot 'System32\ping.exe'` absolutely.
- Extended the landed fix (`launcher_mcp_provision.tests.ps1:851`) to the two remaining sites:
  - `tests/launch_watcher_for_grepai.tests.ps1` — inside the generated-parent here-string, `'/c','"$ping" -n 300 127.0.0.1'`.
  - `tests/launcher_watcher_teardown.tests.ps1` — two sites (was :20 and :182); precomputed `$pingArg` so the nested quotes stay readable inside the `-Command` string.
- Verified green after the edit: teardown **14 [+], 0 [-]**; grepai **19 passed, 0 failed**.
- **Quirk did NOT reproduce from a PowerShell-tool session**, at depth 1 (`session -> cmd.exe`) or depth 2 (`session -> pwsh -> cmd.exe`): bare `ping` ran fine at both depths (probe: `temp/probe_ping.ps1`, `temp/probe_ping2.ps1`). So the absolute path is defensive, not a demonstrated repair — the original failure was observed under the python `run_pester_suite.py` path. Don't claim the fix "fixes" a reproducible bug.
- **PATH**: added `C:\WINDOWS\System32\WindowsPowerShell\v1.0` to the **HKCU** `Environment` Path (prepended, canonical casing, 32→33 entries, backup `%TEMP%\user_path.bak.txt`). It was already on the HKLM Path and already resolving, so this makes it survive contexts that get a user-only/rebuilt PATH.
- **All 5 pane/grid suites already pass** (`launcher_watcher_panes` 7, `launcher_pane_exit_qfy` 8, `launcher_pane_heal_idle` 6, `launcher_grid_reset_attribution` 13, `launcher_pane_line_cap` 1). The real reason they "can't run" from here is the **Bash tool hard-blocks any command line containing the Windows shell name** ("Invoking PowerShell from Bash bypasses PowerShell security checks") — drive them through the PowerShell tool, or put the command in a file and execute that. `reg.exe` and `wsl.exe` are similarly blocked → use Python `winreg`.
- Pester here is **6.0.1**; `-Output` is ambiguous and `-Show` does not exist — call `Invoke-Pester -Path <file>` with no output flag. Pester 3.4.0 suites (teardown) `Write-Host` their results, so `| Out-String` captures **nothing** and `[+]`/`[-]` counts come back 0 — read the printed output, don't trust a captured count.

## Naming re-confirmed and pushed to every memory layer: backend = Graft, daemon binary = graftd
Operator asked for this to be persisted to local memory **plus** AutoMem, Graphiti, heimdall (`kb-insert "memory"`) and mnemosyne, grouped by repository. Three independent sources agree:
1. **upstream README** (`ArihantDeva/heimdall`) — "reference backend: Graft", "MIT licensed, Graft attributed", backend **vendored at `vendor/graft/` (Apache 2.0, NanoNets)**;
2. **upstream `bin/kb-health.sh:98`** — prints `graft config valid (graftd --check-config)` → noun = graft, binary = graftd;
3. **this box** — `~/.heimdall/config.json` reads `"backend": "graft"`.
- Rule: **everywhere I wrote "graftd" as the name of the backend, read it as Graft (daemon binary graftd).**
- Grouped by repository: **MCP-Watchers** (this repo — the misnaming happened here; code corrected in `95fdfb3`, `watcher_pane_scripts.ps1` heimdall branch + `watcher_patterns.ps1`) and **ArihantDeva/heimdall** (upstream, where the evidence lives). Not a VAD fact.
- Still three *unrelated* "graft" programs to disambiguate: (a) this repo's `graft/` + the npm `graft` MCP CLI v0.18.0 (no daemon subcommand), (b) Toolport server id `graft`, (c) heimdall's backend Graft (`~/.graft/`, `graftd.exe`).
- Landed in `MEMORY-mcp-stack.md` § Heimdall (with the `vendor/graft/` Apache-2.0/NanoNets detail added) and `MEMORY.md` § Watchers.

### Where the remote memory writes landed (verified, not assumed)
- **heimdall `kb_insert` — OK.** Wrote `C:\Users\yuni\heimdall-notes\facts\2026-09-21-naming-heimdall-s-backend-is-graft-graftd-is-only-its-daemon-binary.md` (note: heimdall stamps its own date, 09-21). **Verified by `kb_search`**: ranks **#1, [STRONG] cov100%** for "heimdall backend Graft graftd daemon binary naming".
- **Graphiti `add_memory` — OK, async.** Queued into `group_id=mcp-watchers`; the server processes episodes in the background, so "queued" is the only receipt available at write time.
- **AutoMem `store_memory` — OK, with a caveat.** Memory ID `7ea5f46d-0912-43a3-9882-8fae8990a665`; stored as `type=Decision`, importance 0.85, tags incl. both repo names. **Server warned: 1535 chars exceeds the recommended 500 and "backend may auto-summarize"** — so re-recall may return a condensed version. Keep future AutoMem entries short.
- **mnemosyne — FAILED, unreachable.** `toolport_status` shows it *enabled but exposing 0 tools* (same class as serena/beads/atlas/lean-ctx/headroom today). This is the **wedged-gateway "no route"** condition, not a mnemosyne fault. Remediation = gateway restart (Toolport UI / restart WorkBuddy); **do not kill the gateway process** (it won't auto-respawn). Re-run the insert after that.
- **jcode native memory — not applicable.** This session is WorkBuddy, not jcode; jcode is merely installed (`AppData\Local\jcode\bin`). Skipped per the "if running in jcode" condition.

## Graft disambiguation VERIFIED on-box: the CLI and the backend are NOT the same program
Operator asked for it to be proved rather than assumed, with the test: *if they were one program, the CLI would have the Toolport MCP entry AND be heimdall's backend.* Half one holds, half two fails → **two distinct programs, three names**.
- **CLI = `@nanonets/graft` v0.18.0**, `bin: {graft: "dist/cli.js"}` (Node/JS), at `J:/Programs/npm-global/node_modules/@nanonets/graft`; shims at `J:/Programs/npm-global/graft` and `.declick/bin/graft`. Commands: telemetry, version, upgrade, build, ask, skeleton, check, stats, viz, mcp, callers, blast, grep, map, init, uninstall, brain, help. **No `daemon`** — operator's recollection confirmed. Package ships **no `graftd`**.
- **Toolport `graft` is that same CLI** (entry `graft [stdio] graft mcp`); its 6 tools are exactly the CLI's documented MCP tools (`graft_find_code/trace_calls/find_all/file_api/repo_map/check_freshness`). So "Toolport server id graft" is **not a third program** — it is the CLI's MCP registration.
- **heimdall's backend is a separate C program**: `vendor/graft/` = CMake/C subtree (`CMakeLists.txt`, `src/{…,daemon,embed,…}`, `include/`, `third_party/`), a git subtree of the **graft-cpp fork of github.com/NanoNets/Graft, Apache 2.0**, pinned `v0.1.0-heimdall.2`, per its `VENDORED.md`. Builds `graftd.exe` (8,230,676 B). `~/.graft/config.yaml` (`daemon.socket_path`, `embedding.model_path: bge-m3.gguf`) is its config; `start-graftd.bat` runs `graftd.exe --config …`; heimdall's `bin/kb-health.sh`, `kb-rebuild.sh`, `lib/{cli-main,graft-build,setup}.mjs`, `postinstall.mjs` invoke **`graftd`**, never `graft`.
- **Root cause of the confusion**: npm scope `@nanonets` == backend upstream **NanoNets/Graft**. Same vendor, different languages and codebases.
- Note `~/.graft/build.log` reads `embed-index: only 4.0GB free (<4.5GB), skipping build.`

## Heimdall's embed-index free-space gate is WRONG — do not blame a full disk
- A follow-up `kb_search` returned `SEMANTIC_ERROR: embed-index: only 0.5GB free (<2.0GB), skipping query` → **lexical-only results, `sem_coverage=degraded`**, and it did not surface the fact inserted minutes earlier (the file itself is on disk, 2303 B, verified by path).
- **Measured free space at that moment: C: 618.9 GB, J: 61.4 GB.** So the "0.5GB free" figure is a misreport, not disk pressure. Same class as `~/.graft/build.log`'s "4.0GB free (<4.5GB), skipping build".
- Consequence: heimdall recall is lexical-only right now → **verify a `kb_insert` by its returned path, not by searching for it.** Recorded in `MEMORY-mcp-stack.md` § Heimdall.
- **CORRECTION (later the same day): I misread that gate.** It is **free RAM**, not disk — `embed-index.py:67-68` `MIN_FREE_GB=2` / `BUILD_HEADROOM_GB=0.5` with `_free_ram_gb()`. My "gate is wrong / disk is fine" line above is a misdiagnosis: I measured disk (618.9 GB free) when the gate measures RAM. The practical consequence (lexical-only recall, verify inserts by path) is unchanged, but the cause is RAM pressure, not a bug. Fixed in `MEMORY-mcp-stack.md`.

## mcp-agent-mail: it IS managed by this program, and its management has been thrashing for 7 days
Operator asked whether mcp-agent-mail is managed here. **Yes** — `###1.watchers_for_...ps1` has all three of:
start job `$mailMcpJobScript` (:4699-4777), `Start-BackendSupervisor -Name 'mail' -Port 8765` (:5130), duplicate reaper (:5082-5108).
It is **absent from the provision layer** (`watcher_mcp_provision.ps1:690-695` covers only memtrace/grepai/graphenium/graphify-rs/repowise/graft).
- **New finding (the real story): `%LOCALAPPDATA%\mcp-agent-mail\supervisor.log` = 18,562 lines since 2026-09-15T23:48 → 9,657 relaunch/dead events, 4,624 "reaped duplicate mail PID", 4,145 "alive again".** ~1 death+relaunch per minute for 7 days. Port 8765 probes LISTENING, so a port check hides it — only the log shows it. Tail sampled 06:50-06:53 shows dead→relaunch→alive→reap-dup→dead cycling every ~30-60 s.
- Likely mechanism candidates: two starters fighting (start job + supervisor + possible external MCPHttpWatchdog/logon shortcut), or run_server.cmd spawning a child with the same `mcp_agent_mail` token so the reaper kills parent/child, or a genuine ~60 s crash. **Not yet diagnosed.**
- Reaper self-harm hypothesis is strong independently: a relaunch that hasn't bound :8765 yet isn't the port owner → eligible to be reaped → self-inflicted loop. Same pattern for claude-mcp :8080 and graphiti-embed :8003 (:5087-5088); netstat shows matching TIME_WAIT churn on 8080.
- This is probably part of why today's sweep saw mcp-agent-mail refused → connected(41) → dropped. Not only the wedged gateway.
- **Doc conflict to resolve**: standing memory line "MCPHttpWatchdog is the ONLY starter for :8765/:8080" contradicts the code supervising both. One is wrong.
- **Sandbox blockers hit while investigating** (record so nobody re-tries): `schtasks.exe` on the program blacklist; `HKLM\...\Schedule\TaskCache\Tree` → WinError 5; `C:\Windows\System32\Tasks` enumerates 0 files; the PS tool returned empty stdout. Scheduled-task inventory therefore **unverified** — use Task Scheduler GUI / Toolport UI / a peer session.

### Beads created (bd sync done, Dolt auto-commit on)
Epic **mcpw-ymo** P1 + children .1 diagnose, .2 one-starter, .3 reaper grace window (P1); .4 provision coverage, .5 regression test, .6 re-verify (P3 OPTIONAL).
Standalone: **mcpw-sld** P2 gateway restart + re-verify 31; **mcpw-nzc** P3 prune sprawl; **mcpw-i21** P3 confirm headroom after restart.
8 dep edges: .2/.3←.1; .4←.2; .5←.3; .6←.2,.3,sld; i21←sld. 6 blocked, 18 open total (was 8).

## mcpw-qxj.3 — Graft does NOT need to be running (investigated, evidence-based)
Operator: "check if this actually needs to be running." **It does not — nothing on the live path uses graftd.**
- Architecture: **journal authoritative, graph is a projection** (`lib/journal.mjs`, `lib/sink.mjs`). Projection is regenerated by `graft build` (npm CLI), not per insert.
- The daemon's `graft not found — journal will be maintained, projection deferred` (`heimdall-reconciler.mjs:70`) is the **npm `@nanonets/graft` CLI**, probed at `existsSync($GRAFT || ~/.local/bin/graft)`. `~/.local/bin/graft` is **missing** here (only `graftd.exe` sits there) and `$GRAFT` is unset → hence the message. **That, not graftd, is what's actually missing**, and the fix is one line (`GRAFT=<path>` or drop the CLI there).
- `GraftSink.insert()` does **no I/O** — content-hash id only; "we do NOT call `graft insert` (that API no longer exists)". Retrieval = `graft ask --json` + local `embed-index`; `kb_insert` writes `~/.heimdall-notes/facts/*.md`. Neither touches graftd.
- `graft.db` (graftd's store) is touched only by admin scripts (`kb-rebuild.sh`, `kb-rehome.sh`, `kb-stale-scan.py`, `seed-graft.sh`); here it is 4096 B, untouched since 2026-09-16.
- Measured with graftd dead (no process, stale socket 21/09): `heimdall daemon --once` → **RC=0** watching 2 roots; `doctor` → HEALTHY RC=0; `journal.db` 73,728 B; MCP `kb_search`/`kb_insert` both work.
- **Recommendation: close/reframe qxj.3.** Don't build a graftd supervisor for heimdall's sake. (Standing one-starter rule still applies if graftd is ever started.)
